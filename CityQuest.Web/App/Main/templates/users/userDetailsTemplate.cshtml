<div class="modal-header">
    <div class="row row-centered">
        <div class="col-xs-12 text-center">
            <button type="button"
                    class="close"
                    ng-click="vm.templateActions.close();">
                &times;
            </button>
            <label class="cq-modal-title-label">
                {{vm.title}}
            </label>
        </div>
    </div>
</div>
<div class="modal-body">
    <div class="row cq-control-row">
        <div class="col-xs-2 cq-control-label">
            @L("UserName"): &nbsp;
        </div>
        <div class="col-xs-8">
            <input class="form-control cq-control"
                   ng-disabled="true"
                   ng-readonly="true"
                   ng-model="vm.entity.userName" />
        </div>
    </div>
    <div class="row cq-control-row">
        <div class="col-xs-2 cq-control-label">
            @L("Name"): &nbsp;
        </div>
        <div class="col-xs-8">
            <input class="form-control cq-control"
                   ng-disabled="!(vm.templateMode.isCreate() || vm.templateMode.isUpdate())"
                   ng-readonly="!(vm.templateMode.isCreate() || vm.templateMode.isUpdate())"
                   ng-model="vm.entity.name" />
        </div>
    </div>
    <div class="row cq-control-row">
        <div class="col-xs-2 cq-control-label">
            @L("Surname"): &nbsp;
        </div>
        <div class="col-xs-8">
            <input class="form-control cq-control"
                   ng-disabled="!(vm.templateMode.isCreate() || vm.templateMode.isUpdate())"
                   ng-readonly="!(vm.templateMode.isCreate() || vm.templateMode.isUpdate())"
                   ng-model="vm.entity.surname" />
        </div>
    </div>
    <div class="row cq-control-row">
        <div class="col-xs-2 cq-control-label">
            @L("Email"): &nbsp;
        </div>
        <div class="col-xs-8">
            <input class="form-control cq-control"
                   ng-disabled="!(vm.templateMode.isCreate() || vm.templateMode.isUpdate())"
                   ng-readonly="!(vm.templateMode.isCreate() || vm.templateMode.isUpdate())"
                   ng-model="vm.entity.emailAddress" />
        </div>
    </div>
    <div class="row cq-control-row">
        <div class="col-xs-2 cq-control-label">
            @L("PhoneNumber"): &nbsp;
        </div>
        <div class="col-xs-8">
            <input class="form-control cq-control"
                   ng-disabled="!(vm.templateMode.isCreate() || vm.templateMode.isUpdate())"
                   ng-readonly="!(vm.templateMode.isCreate() || vm.templateMode.isUpdate())"
                   ng-model="vm.entity.phoneNumber" />
        </div>
    </div>
    <div class="row cq-control-row">
        <div class="col-xs-2 cq-control-label">
            @L("Roles"): &nbsp;
        </div>
        <div class="col-xs-8">
            <select multiple
                    class="form-control cq-control"
                    ng-disabled="!(vm.templateMode.isCreate() || vm.templateMode.isUpdate())"
                    ng-readonly="!(vm.templateMode.isCreate() || vm.templateMode.isUpdate())"
                    ng-model="vm.entity.roles"
                    ng-options="role as role.name for role in vm.allRoles track by role.name" />
        </div>
    </div>
</div>
<div class="modal-footer">
    <button type="button"
            class="btn btn-danger pull-left"
            ng-if="vm.templateAvailableActions.deleteEntity()"
            ng-click="vm.templateActions.deleteEntity();">
        @L("ButtonDelete")
    </button>
    <button type="button"
            class="btn btn-success"
            ng-if="vm.templateAvailableActions.saveEntity()"
            ng-click="vm.templateActions.saveEntity();">
        @L("ButtonSave")
    </button>
    <button type="button"
            class="btn btn-primary"
            ng-click="vm.templateActions.close();">
        @L("ButtonClose")
    </button>
</div>