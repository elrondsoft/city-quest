<div class="container-fluid" ng-controller="app.views.users.userListController as vm">
    <jtable id="userTable"
            name="userTable"
            title="vm.title"
            paging="vm.paging"
            actions="vm.actions"
            messages="vm.messages"
            filters="vm.filters"
            fields="vm.fields"
            records-loaded="vm.recordsLoaded"
            toolbar="vm.toolbar"
            loaded="vm.loaded()">
    </jtable>
</div>